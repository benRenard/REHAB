% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertaintyEstimation.R
\name{plot.predictiveUncertainty}
\alias{plot.predictiveUncertainty}
\title{Plot Predictive Uncertainty}
\usage{
\method{plot}{predictiveUncertainty}(
  x,
  axisValues = data.frame(Index = 1:NROW(x$sim)),
  labs = c(ifelse(is.null(names(axisValues)), " ", names(axisValues)), "Prediction"),
  col = "red",
  alpha = 0.25,
  showRawSim = TRUE,
  ...
)
}
\arguments{
\item{x}{object of class 'predictiveUncertainty', resulting from a call to function [getUncertainty()]}

\item{axisValues}{numeric or date vector, what to display on the x-axis.}

\item{labs}{character vector of size 2, x-axis and y-axis labels.}

\item{col}{color specification, color used for predictions.}

\item{alpha, }{numeric in (0,1), transparency of uncertainty interval.}

\item{showRawSim}{boolean, add raw simulations (before mu-correction) to plot ?}

\item{...}{further plotting arguments passed to or from other methods.}
}
\value{
A ggplot showing the simulated values + uncertainties
}
\description{
Plot predictive uncertainty estimated after a residual analysis.
}
\examples{
w=analyseResiduals(ArdecheRiver$obs[1:366],ArdecheRiver$sim[1:366])
u=getUncertainty(w,sim=ArdecheRiver$sim[367:731])
plot(u,axisValues=ArdecheRiver$date[367:731])
}
